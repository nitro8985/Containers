#include "stack.h"

namespace s21 {
    template <typename T>
    Stack<T>::Stack() {
        
    }

    template <typename T>
    Stack<T>::Stack(std::initializer_list<value_type> const& items) {

    }

    template <typename T>
    Stack<T>::Stack(const Stack& i_container) {

    }

    template <typename T>
    Stack<T>::Stack(const Stack&& i_container) {
        
    }

    template <typename T>
    typename Stack<T>::iterator Stack<T>::begin() {

    }

    template <typename T>
    typename Stack<T>::iterator Stack<T>::end() {
        
    }

    template <typename T>
    bool Stack<T>::empty() {
        
    }

    template <typename T>
    typename Stack<T>::size_type Stack<T>::size() {
        
    }

    template <typename T>
    typename Stack<T>::size_type Stack<T>::max_size() {
        
    }

    template <typename T>
    void Stack<T>::clear() {
        
    }
}
